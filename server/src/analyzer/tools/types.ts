export enum primitive {
    INT = "int",
    VARCHAR = "varchar",
    DATE = "date",
    BOOLEAN = "boolean",
    DOUBLE = "double",
    NULL = "null"
}

export enum arithmeticOperator {
    PLUS = "+",
    MINUS = "-",
    MULT = "*",
    DIV = "/",
    MOD = "%"
}

export enum relationalOperator {
    EQ = "=",
    NEQ = "!=",
    LESS = "<",
    GREATER = ">",
    LEQ = "<=",
    GEQ = ">="
}
