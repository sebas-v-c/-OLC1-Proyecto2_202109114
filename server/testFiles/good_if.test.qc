DECLARE @VAR INT DEFAULT 5;

IF @var = 5 THEN
BEGIN
    SET @VaR = 10;
END;


IF @var < 5 THEN
    SET @VAR = 20 + 5;
ELSE
     declare @var2 varchar;
     set @VAR = 12 + 1;
     set @var2 = "simon";
END IF;
